/* global Tree, describe, it, expect, should */

describe('Tree()', function() {
  var tree;
  beforeEach(function() {
    tree = new Tree();
  });

  it('should make a new tree', function() {
    expect(true).to.equal(false);
  });

  it('should do some other stuff', function() {
    expect(true).to.equal(false);
  });

  it('should do some even more stuff', function() {
    expect(true).to.equal(false);
  });


});
